name: Video Uploader
on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'Direct Link to Video'
        required: true

jobs:
  upload:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Process and Convert Video
        run: |
          # داگرتنی ڤیدیۆکە
          wget --user-agent="Mozilla/5.0" "${{ github.event.inputs.video_url }}" -O input.mp4
          
          # کۆنڤێرتکردنی ڤیدیۆکە بۆ H.264 و دەنگ بۆ AAC بۆ ئەوەی لە هەموو شوێنێک لێبدات
          # ئەم پرۆسەیە کەمێک کاتی دەوێت چونکە ڤیدیۆکە دووبارە دروست دەکاتەوە
          ffmpeg -i input.mp4 -vcodec libx264 -acodec aac -strict experimental -movflags +faststart movie.mp4

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: vid-${{ github.run_number }}
          files: movie.mp4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
